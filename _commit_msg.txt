EVT epochs from [Events] declarations; settings propagate to docks

EVT loader
- Read the [Events] Begin*/End* code declarations (BeginBlock/EndBlock,
  Beginperiod/Enderiod, ...) and build one epoch per Begin/End couple,
  numbered "epoch #1..#n" in declaration then time order, alongside the
  whole-measurement "experiment" epoch.  example1.EVT now yields experiment
  + epoch #1 (the 11/12 block) + epoch #2/#3 (the two 21/22 periods), with no
  code-selection dialog.  Files without declared codes keep the old fallback.

Settings propagation
- _on_workspace_changed now refreshes every dock's workspace reference and
  notifies DataChange.PARAMS, so PSD / profile / transfer / spectrogram /
  results recompute when the analysis settings change.

Poincare: drop the legend (the per-epoch checkboxes already serve as one).

401 passing.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
