Epoch editor: V2-faithful y-axis, tab20 colours, rename/delete

Re-style the epoch editor to match V2 and keep the development improvements:
- Epoch names down the y-axis (clickable tick labels).
- One tab20 colour per epoch (active full, inactive dimmed) with vertical
  start/end labels at the bar edges.
- Click a y-axis label to rename the epoch, or clear the name to delete it.
- (Kept) edge-drag to resize, body-click to toggle active.

Every edit commits into session.epochs and emits epochsChanged.  401 passing.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
