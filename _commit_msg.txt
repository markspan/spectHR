Make Poincare epoch bar vertical and scrollable (V2 layout)

The epoch-scheduling checkboxes sat in a horizontal row below the
scatter; with many epochs they overflowed off the edge.  Match V2's
poincarePlotWidget: a horizontal split with the plot on the left
(stretch 3) and a vertical checkbox column in a scroll area on the
right (stretch 1), so a recording with many epochs stays usable.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
